dev-help() {
    echo "💡 DEV COMMAND CHEAT SHEET"
    echo "-----------------------------------"
    echo "Python: py-start | py-run   | py-add <package>"
    echo "Go:     go-start | go-run   | go-build"
    echo "Rust:   rust-start | rust-run | rust-test"
    echo "React:  react-start"
    
    echo "💡 GIT SHORTCUT CHEAT SHEET"
    echo "-----------------------------------"
    echo "Setup:  git-setup (Runs your global git.sh configuration)"
    echo "Status: gs        (git status)"
    echo "Stage:  ga <file> (git add file)  | gaa (git add .)"
    echo "Commit: gc \"msg\"  (git commit -m)"
    echo "Sync:   gp        (git push)      | gl  (git pull)"
    echo "Diff:   gd        (git diff)"
}

